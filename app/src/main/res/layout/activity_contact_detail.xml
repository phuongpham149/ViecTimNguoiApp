<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imgBack"
        android:contentDescription="@string/img_contact_detail_description"
        android:layout_width="@dimen/img_back_contact_detail"
        android:layout_height="@dimen/img_back_contact_detail"
        android:layout_marginLeft="@dimen/img_back_contact_detail_margin_left"
        android:layout_marginStart="@dimen/img_back_contact_detail_margin_left"
        android:layout_marginTop="@dimen/img_back_contact_detail_margin_top"
        android:src="@drawable/ic_back" />

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginLeft="@dimen/scroll_contact_detail_margin"
        android:layout_marginRight="@dimen/scroll_contact_detail_margin"
        android:layout_weight="20">

        <LinearLayout
            android:id="@+id/layoutContact"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

        </LinearLayout>
    </ScrollView>

    <include
        layout="@layout/contact_message_area"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/message_detail_contact_margin_top"
        android:gravity="bottom" />
</LinearLayout>